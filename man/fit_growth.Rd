% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Make_Growth.R
\name{fit_growth}
\alias{fit_growth}
\title{Fit growth model (complete workflow)}
\usage{
fit_growth(
  parameters,
  datain,
  random = NULL,
  map = list(),
  control = list(),
  silent = FALSE
)
}
\arguments{
\item{parameters}{Initial parameters list}

\item{datain}{Model data list}

\item{random}{Character vector of random effects or NULL for no random effects (default: NULL)}

\item{map}{Parameter map list for fixing parameters (default: empty list)}

\item{control}{Optimization control list passed to nlminb (default: empty list)}

\item{silent}{Suppress RTMB output? (default: FALSE)}
}
\value{
List containing obj (RTMB object), opt (optimization result),
sdr (sdreport if successful), and converged (logical)
}
\description{
Fit growth model (complete workflow)
}
\examples{
\dontrun{
# Without random effects
fit <- fit_growth(parameters, datain)

# With random effects
fit <- fit_growth(parameters, datain, random = c("MerrorRel", "MerrorRec"))

# With parameter mapping
map <- list(sigGrow = factor(NA))  # Fix sigGrow
fit <- fit_growth(parameters, datain, map = map)
}
}
